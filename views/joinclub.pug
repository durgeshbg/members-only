extends layout 

block content 
  h1= title
  if errors 
    each error in errors 
        p.error #{error.msg}
  form(method="post")  
    .form-row.long
      label(for="key") Enter the secret key: 
      input(type="hidden" name="user_id" value=user._id)
      input#key(type="text" name="key" placeholder="Hint:fastest sorting algorithm")
    button(type="submit") Submit